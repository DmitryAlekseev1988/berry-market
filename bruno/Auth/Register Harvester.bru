meta {
  name: Register Harvester
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "harvester@example.com",
    "password": "Password123",
    "name": "Иван Заготовитель",
    "userType": "HARVESTER"
  }
}

script:post-response {
  if (res.body.token) {
    bru.setVar("authToken", res.body.token);
  }
}
