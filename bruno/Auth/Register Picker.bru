meta {
  name: Register Picker
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "picker@example.com",
    "password": "Password123",
    "name": "Мария Сборщик",
    "userType": "PICKER"
  }
}

script:post-response {
  if (res.body.token) {
    bru.setVar("authToken", res.body.token);
  }
}
